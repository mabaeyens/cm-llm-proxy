# Dependencies
node_modules/
npm-debug.log*

# Certificates (generate locally, do not commit real certs)
# certs/*.pem  ← placeholder files are committed; replace with real certs locally

# Environment
.env
*.env

# macOS
.DS_Store

# Logs
*.log
