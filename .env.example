# Configuración del proxy — copia a .env y ajusta los valores:
#     cp .env.example .env

# Origen permitido por CORS (tu servidor Qlik Sense)
QLIK_ORIGIN=https://your-qlik-server

# Puerto del proxy (por defecto 3000)
PORT=3000
